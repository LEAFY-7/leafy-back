<?xml version="1.0" encoding="UTF-8"?>
    <!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

    <mapper namespace="bucheon.leafy.application.mapper.QnaCommentMapper">

        <delete id="deleteAll">
            DELETE FROM qna
        </delete>

        <delete id="delete" parameterType="map">
            DELETE FROM qna WHERE id = #{id} and user_id = #{user_id}
        </delete>

        <insert id="insert" parameterType="QnaCommentDto">
            INSERT INTO qna
                (title, contents, id, qan_status)
            VALUES
                (#{title}, #{contents}, #{id}, #{qan_status})
        </insert>

        <select id="selectAll" resultType="QnaCommentDto">
            SELECT id, created_at, modified_at, is_delete ,contents, qna_status, title, user_id
            FROM qna
            ORDER BY created_at DESC, id DESC
        </select>

        <sql id="selectFrom">
            SELECT id, created_at, modified_at, is_delete ,contents, qna_status, title, user_id
            FROM qna
        </sql>

        <select id="select" parameterType="int" resultType="QnaCommentDto">
            <include refid="selectFrom"/>
            WHERE id = #{id}
        </select>

        <select id="selectPage" parameterType="map" resultType="QnaCommentDto">
            <include refid="selectFrom"/>
            ORDER BY created_at DESC, id DESC
            LIMIT #{offset}, #{pageSize}
        </select>

        <update id="update" parameterType="QnaCommentDto">
            UPDATE qna
            SET   title = #{title}
              , contents = #{contents}
              , modified_at = now()
            WHERE user_id = #{user_id}
        </update>


        <sql id="searchCondition">
            <choose>
                <when test='option=="T"'>
                    AND title LIKE concat('%', #{keyword}, '%')
                </when>
                <when test='option=="U"'>
                    AND user_id LIKE concat('%', #{keyword}, '%')
                </when>
                <otherwise>
                    AND (title   LIKE concat('%', #{keyword}, '%')
                    OR   contents LIKE concat('%', #{keyword}, '%'))
                </otherwise>
            </choose>
        </sql>

        <select id="searchSelectPage" parameterType="SearchCondition" resultType="QnaCommentDto">
            SELECT id, created_at, modified_at, is_delete ,contents, qna_status, title, user_id
            FROM qna
            WHERE true
            <include refid="searchCondition"/>
            ORDER BY created_at DESC, id DESC
            LIMIT #{offset}, #{pateSize}
        </select>

    </mapper>
