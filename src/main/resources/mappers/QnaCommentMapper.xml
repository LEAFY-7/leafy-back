<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="bucheon.leafy.application.mapper.QnaCommentMapper">

    <update id="deleteByQnaCommentId" parameterType="Long">
        UPDATE  qna_comment
           SET is_delete = 1
         WHERE qna_comment_id = #{qnaCommentId}
           AND user_id = #{userId}
    </update>

    <insert id="save" parameterType="QnaCommentSaveReqeust">
        INSERT INTO qna_comment
            ( created_at, modified_at, is_delete, comment, user_id  )
        VALUES
            (now(), now(), 0, #{comment}, #{userId})
    </insert>

    <update id="editByQnaCommentId" parameterType="QnaCommentEditReqeust">
        UPDATE qna_comment
        SET comment = #{comment}
          , modified_at = now()
        WHERE qna_comment_id = #{qnaCommentId}
          AND user_id = #{userId}
    </update>

</mapper>
