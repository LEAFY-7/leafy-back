<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="bucheon.leafy.application.mapper.QnaCommentMapper">

    <select id="count" parameterType="int" resultType="int">
        SELECT count(*)
          FROM qna_comment
    </select>

    <delete id="deleteByQnaCommentId" parameterType="map">
        DELETE
          FROM qna_comment
         WHERE qna_comment_id = #{qnaCommentId}
           AND user_id = #{userId}
    </delete>

    <insert id="save" parameterType="QnaCommentDto">
        INSERT INTO qna_comment
            ( created_at, is_delete, comment, user_id, qna_id)
        VALUES
            (now(), 0, #{comment}, #{userId}, #{qnaId})
    </insert>

    <select id="selectAll" resultType="QnaCommentDto">
        SELECT qna_comment_id, created_at, modified_at, is_delete, comment, user_id, qna_id
        FROM qna_comment
    </select>

    <select id="pageFindByQnaCommentId" parameterType="long" resultType="QnaCommentDto">
        SELECT *
        FROM qna_comment
        WHERE qna_comment_id = #{qnaCommentId}
    </select>

    <update id="editByQnaCommentId" parameterType="QnaCommentDto">
        UPDATE qna_comment
        SET comment = #{comment}
          , modified_at = now()
        WHERE qna_comment_id = #{qnaCommentId}
          AND user_id = #{userId}
    </update>

    <select id="selectId" parameterType="long" resultType="QnaCommentDto">
        SELECT qna_comment_id
        FROM qna_comment
    </select>
</mapper>
