<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="bucheon.leafy.application.mapper.FeedReplyMapper">
    <select id="findReplyList" resultType="bucheon.leafy.domain.feed.dto.response.FeedReplyResponse">
        select * from feed_reply where is_delete = false
    </select>
</mapper>