<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="bucheon.leafy.application.mapper.QnaReplyMapper">

    <delete id="deleteAllReply">
        DELETE FROM reply
    </delete>

    <select id="count" parameterType="int" resultType="int">
        SELECT count(*) FROM reply
        WHERE  rid = #{rid}
    </select>

    <delete id="delete" parameterType="map">
        DELETE FROM reply WHERE rid = #{rid} AND user_id = #{user_id}
    </delete>

    <insert id="insert" parameterType="QnaReplyDto">
        INSERT INTO reply
            (reply_reply, user_id, reply, created_at, modifyed_at)
        VALUES
            (#{reply_reply}, #{user_id}, #{reply}, now(), now())
    </insert>

    <select id="selectAll" parameterType="int" resultType="QnaReplyDto">
        SELECT reply_reply, rid,  reply, user_id, created_at, modifyed_at
        FROM reply
        WHERE reply_reply = #{reply_reply}
        ORDER BY created_at ASC, rid ASC
    </select>

    <select id="select" parameterType="int" resultType="QnaReplyDto">
        SELECT reply_reply, rid, reply, user_id, created_at, modifyed_at
        FROM reply
        WHERE rid = #{rid}
    </select>

    <update id="update" parameterType="QnaReplyDto">
        UPDATE reply
        SET user_id = #{user_id}
          , modifyed_at = now()
        WHERE rid = #{rid} and user_id = #{user_id}
    </update>
</mapper>
