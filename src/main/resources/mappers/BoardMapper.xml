<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="bucheon.leafy.application.mapper.BoardMapper">

    <select id="findBoardList" resultType="Board">
        select * from board;
    </select>

    <select id="findBoardById" resultType="Board" parameterType="java.lang.Long">
        select * from board where board_id = #{id}
    </select>

    <insert id="saveBoard" parameterType="Board"  useGeneratedKeys="true" keyProperty="board_id">
        insert into board (title,contents,is_hide) values (#{title}, #{contents}, #{isHide})
    </insert>

    <update id="editBoard"  parameterType="Board">
        update board set title = #{title}, contents = #{contents}, is_hide = #{isHide} where board_id = #{board_id}
    </update>

    <delete id="deleteBoard">
        delete from board where board_id = #{id}
    </delete>

</mapper>