<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="bucheon.leafy.application.mapper.FeedCommentMapper">

    <select id="findCommentList" resultType="FeedComment">
        select * from feed_comment
    </select>

    <insert id="saveComment" parameterType="FeedComment">
        insert into feed_comment(comment,feed_id,user_id,is_delete,created_at,modified_at)
        values (#{comment}, #{user.id}, #{feed.id}, false, sysdate(), sysdate())
    </insert>

    <update id="editComment" parameterType="FeedComment">
        update feed_comment set comment = #{comment}, modified_at = sysdate() where comment_id = #{id}
    </update>

    <delete id="hardDeleteComment"  parameterType="java.lang.Long">
        delete from feed_comment where comment_id = #{id}
    </delete>

    <update id="softDeleteComment" parameterType="java.lang.Long">
        update feed_comment set is_delete = true where comment_id = #{id}
    </update>

</mapper>