<?xml version="1.0" encoding="UTF-8"?>
    <!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

    <mapper namespace="bucheon.leafy.application.mapper.NoticeMapper">


        <delete id="deleteAll">
            DELETE FROM notice
        </delete>

        <delete id="deleteQna" parameterType="map">
            DELETE FROM notice WHERE id = #{id} and id = #{id}
        </delete>

        <insert id="insert" parameterType="NoticeDto">
            INSERT INTO notice
                (title, contents, id)
            VALUES
                (#{title}, #{contents}, #{id})
        </insert>

        <select id="selectAll" resultType="NoticeDto">
            SELECT id, title, contents, id, view_cnt, comment_cnt, created_at, user_user_id
            FROM notice
            ORDER BY created_at DESC, id DESC
        </select>

        <sql id="selectFrom">
            SELECT id, title, contents, id, view_cnt, comment_cnt, created_at, user_user_id
            FROM notice
        </sql>

        <select id="select" parameterType="int" resultType="NoticeDto">
            <include refid="selectFrom"/>
            WHERE id = #{id}
        </select>

        <select id="selectPage" parameterType="map" resultType="NoticeDto">
            <include refid="selectFrom"/>
            ORDER BY created_at DESC, id DESC
            LIMIT #{offset}, #{pageSize}
        </select>

        <update id="update" parameterType="NoticeDto">
            UPDATE notice
            SET   title = #{title}
              , contents = #{contents}
              , modified_at = now()
            WHERE user_id = #{user_id}
        </update>


        <sql id="searchCondition">
            <choose>
                <when test='option=="T"'>
                    AND title LIKE concat('%', #{keyword}, '%')
                </when>
                <when test='option=="U"'>
                    AND user_id LIKE concat('%', #{keyword}, '%')
                </when>
                <otherwise>
                    AND (title   LIKE concat('%', #{keyword}, '%')
                    OR   contents LIKE concat('%', #{keyword}, '%'))
                </otherwise>
            </choose>
        </sql>

        <select id="searchSelectPage" parameterType="SearchCondition" resultType="NoticeDto">
            SELECT id, title, contents, user_id, view_cnt, comment_cnt, created_at, user_user_id
            FROM notice
            WHERE true
            <include refid="searchCondition"/>
            ORDER BY created_at DESC, id DESC
            LIMIT #{offset}, #{pateSize}
        </select>

        <select id="searchResultCnt" parameterType="SearchCondition" resultType="int">
            SELECT count(*)
            FROM notice
            WHERE true
            <include refid="searchCondition"/>
        </select>
    </mapper>
